import { Tag } from "./style";

export function NeutralTag() {
    return (
        <Tag title="Pagamento de fatura ou transferência entre as suas contas: não entra em receitas nem despesas">
            fora dos totais
        </Tag>
    );
}
